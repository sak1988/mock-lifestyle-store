import { NextResponse } from 'next/server';export async function POST(req){const body=await req.json();console.log('EVENT',body?.name,{session:body?.session_id,at:body?.ts});return NextResponse.json({ok:true});}